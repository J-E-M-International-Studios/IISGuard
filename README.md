# IISGuard
IISGuard is a self-contained .NET application that monitors IIS logs in real time and automatically blocks abusive IP addresses.   It is designed for administrators who want to protect their IIS servers against brute force attempts, bots, and excessive POST/ERROR traffic.
